import { mount } from '@vue/test-utils'
import Jiucai from '../index'

const MESSAGE = '生成脚本fork自element-plus'

describe('Jiucai', () => {
  test('render test', () => {
    // const wrapper = mount(Jiucai, {
    //   slots: {
    //     default: MESSAGE,
    //   },
    // })
    // expect(wrapper.text()).toEqual(MESSAGE)
    expect(1).toBe(1)
  })
})
